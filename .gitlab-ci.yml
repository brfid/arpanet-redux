image: python:3.11

stages:
  - test

variables:
  GIT_DEPTH: "0"

source-and-tests:
  stage: test
  interruptible: true
  script:
    - python3 scripts/check-source-only.py --history "$CI_COMMIT_SHA"
    - python3 -m unittest discover -s tests -v
    - sh tests/test_runtime.sh
