# Unified passive NCC coexistence desk

- **Date:** 2026-09-01
- **Retained read-only result:** `/Users/brf/src/arpanet-redux-lab/results/ncc-pdp11-its-coexistence-canonical-20260901T153758Z`
- **Result repository checkpoint:** `982462e12de6fdc70bc1a73a1b3871377fb7ac9c`
- **Scope:** Completed structured-artifact projection and loopback presentation only

## Question

Can one passive operator desk explain the accepted Network UNIX-to-ITS application result, the still-incomplete typed message journey, and the NCC report evidence from the same seven-component composition without changing a persisted schema, parsing a raw simulator log, inferring activity on configured links, or giving the display simulator authority?

## Implementation

`ncc.coexistence_display` reads the existing shared topology, manifest, application evidence, cleanup evidence, outcome, composition verdict, historical-event sidecar, and typed journey sidecar. It validates fixed artifact paths and available manifest digests, exact run and topology identities, terminal lifecycle facts, the application requirements, the existing journey reducer result, direct Type 303 and Type 302 event counts, and the saved reciprocal-line support. It replays the existing line reducer in memory and requires the verdict's two sequences to be the event stream's latest observed `up` support pair.

The snapshot is an in-memory presentation contract, not a new persisted schema. It retains the six unmapped links as configured-only, projects Gate 4H as a separate application conclusion, embeds the existing typed journey projection without filling either missing host-ingress boundary, and separates the composition verdict's accepted direct-line state from a deterministic run-finish reduction of the later receiver tail.

`ncc.coexistence_server` binds only IPv4 loopback and serves one self-contained page plus one resolved snapshot through GET and HEAD. It has no arbitrary-file route or mutation method. The browser draws the fixed topology and evidence phase rail from the Python-resolved snapshot; it does not parse artifacts, correlate messages, reconcile endpoints, or align simulator clocks.

The presentation uses a project-authored visual grammar informed by the 1972 SRI/NIC *Scenarios for Using the ARPANET* demonstration booklet: an ochre cover, visible comb binding, typewritten folio body, numbered result procedure, underlined interactive keys, dotted rules, sparse line diagrams, and italic annotations. This is an adaptation of the booklet's documentary hierarchy, not a claim that an NCC operator screen looked like the booklet. No scan image, OCR, traced ornament, or source glyph is embedded; the source review and redistribution boundary are recorded in [ICCC 1972 demonstration-booklet visual grammar](../research/2026-09-01-iccc-scenarios-visual-grammar.md).

## Retained replay

The retained accepted result validated without modification. The adapter reproduced application `passed`, ten typed observations, `missing-boundary` at `boundary:request:6`, Type 303 counts of 15/8/13/8 and Type 302 counts of 14/7/13/7 for IMPs 5/6/7/62, and accepted reciprocal `up` support sequences 322 and 355. It retained five later mapped endpoint observations at sequences 377, 399, 421, 443, and 475. At the manifest's run-finish time, the existing reducer classified IMP 5's latest direct endpoint as `down`, IMP 6's last `up` endpoint as `stale`, and the paired line as `stale`; none of those later classifications replaced the accepted composition verdict.

The historical sidecar has no digest entry in this run's manifest. The adapter therefore states that limitation instead of inventing one and binds the stream through its validated complete record, run identity, interface identity, embedded topology, report counts, and exact verdict support. The application and cleanup text artifacts likewise have no manifest digests and remain bound by their required facts plus the passing evaluator checks.

## Verification

Source-only tests construct a complete synthetic result and cover deterministic serialization, the independent application/journey/line conclusions, the post-support tail, configured-only links, manifest digest mismatch, report-count disagreement, verdict-support disagreement, GET/HEAD-only transport, loopback binding, presentation-only JavaScript, keyboard-native controls, and reduced-motion styling. The complete Python suite passed with 179 tests and one expected sandbox UDP-bind skip; `make test` also completed with the expected runtime Unix-domain socket skip.

The retained result was also served on loopback and inspected at 1280-by-720 desktop and 390-by-844 mobile viewports. Visual QA verified that the exact 322/355 support pair remains the phase rail's center of attention, later direct reports retain a different color and authority, both missing journey boundaries remain selectable, topology labels remain legible, and phase and boundary selection produce no browser console error. At 390 pixels the document itself remains exactly viewport-wide while the long phase rail, topology, route, and boundary sequence retain explicit local horizontal scrollers.

## Finding and limit

The completed composition can now hand an operator one evidence-backed diagnosis without merging its evidence planes: the application passed, the typed journey still stops at `boundary:request:6`, the mapped line satisfied the composition verdict earlier, and later receiver observations describe teardown-era state rather than an application failure.

The supported harness persists no controller-exit sequence in the historical-event stream. The desk therefore uses historical event sequence only to distinguish the exact accepted support from the post-support receiver tail and explicitly marks the controller-exit sequence as unpersisted. It does not place a guessed teardown marker, claim a shared simulator clock, prove application rerouting, add KA10 or IMP11-A ingress evidence, or extend simulator authority.
